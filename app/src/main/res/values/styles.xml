<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/bloomBackground</item>
        <item name="colorPrimaryDark">@color/bloomBackground</item>
        <item name="colorAccent">@color/bloomBackground</item>
    </style>

    <style name="HintText" parent="TextAppearance.Design.Hint">
        <item name="android:textSize">14.4sp</item>
        <item name="android:textColorHint">@color/colorBlack</item>
        <item name="colorControlActivated">@color/colorBlack</item>
        <item name="android:textCursorDrawable">@color/colorBlack</item>

    </style>



</resources>
